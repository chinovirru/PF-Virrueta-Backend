import { describe } from "mocha";

describe('Tests para las rutas de Productos', () => {
    it('Se obtiene un listado de productos', () => {
        try {
            assert(true,true)
            console.log('Test correcto')
        } catch {
            console.log('Fallo el test')
        }
    })
})